# Tags in this repository are bare, with no "v" prefix.
# npm's built-in default for tag-version-prefix is "v", so without this line
# `npm version 7.0.0` would create a tag named "v7.0.0" and break the convention.
tag-version-prefix=
